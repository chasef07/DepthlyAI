/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    // Use static files in public/ as your HTML pages
    trailingSlash: true
  };
  
  module.exports = nextConfig;
  