/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    // Add any experimental flags you really need here
  },
  // No custom webpack fallback override here — let Next.js handle it
}

module.exports = nextConfig

  